- 无 `MSVC` / `MinGW`，`window` 下使用`gcc`等`GNU`工具，替代 [w64devkit](https://github.com/skeeto/w64devkit/releases)
	- 只需设置 `PATH` 环境变量即可，w64devkit 自带的 `lib/` 和 `x86_64-w64-mingw32/lib/` 目录会被 gcc 等自动找到，**无需额外设置**
	- 将 w64devkit 的 `bin` 目录添加到系统 PATH 中，这是唯一**必须**配置的环境变量